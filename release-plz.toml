[workspace]
# Don't publish to crates.io (binary, not a library)
publish = false
# Create GitHub releases with changelog
git_release_enable = true
# Include all commits in changelog (not just conventional commits)
release_commits = "^.*"
